package com.zvm.classfile.attribute.runtimeVisibleAnnotations;

import com.zvm.basestruct.u1;
import com.zvm.basestruct.u2;
import com.zvm.classfile.attribute.runtimeVisibleAnnotations.element_value;

public class const_value_index extends element_value {
public u1 tag;
    public u2 const_value_index;
}
